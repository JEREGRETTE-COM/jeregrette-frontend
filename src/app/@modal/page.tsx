/**
 * Matches "/". A client navigation that no longer matches a slot keeps its last
 * content on screen, so the home page must match explicitly for the dialog to
 * close after a repost redirects there.
 */
export default function ModalHome() {
  return null;
}
